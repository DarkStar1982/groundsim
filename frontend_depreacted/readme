# Ground Simulator Front end

HTML and Vue.js

## Installation

You have to install npm on the box, after doing that, run

```bash
npm install
```

to install all of the dependencies.

## Usage

The index.html needs to be served from a web server, Apache or nginx.